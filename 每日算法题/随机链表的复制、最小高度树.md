题目一：【随机链表的复制】        https://leetcode.cn/problems/copy-list-with-random-pointer/description/?envType=problem-list-v2&envId=hash-table        快手-2023-Java开发        
题目二：【最小高度树】        https://leetcode.cn/problems/minimum-height-trees/?envType=problem-list-v2&envId=depth-first-search        元戎-2024-C++开发
```CPP
class Solution {
public:
    vector<int> findMinHeightTrees(int n, vector<vector<int>>& edges) {
        // 特殊情况：只有一个节点
        if (n == 1) return {0};
        
        // 构建邻接表和度数数组
        vector<vector<int>> adj(n);
        vector<int> degree(n, 0);
        
        for (const auto& edge : edges) {
            int u = edge[0], v = edge[1];
            adj[u].push_back(v);
            adj[v].push_back(u);
            degree[u]++;
            degree[v]++;
        }
        
        // 初始化队列，加入所有叶子节点（度数为1）
        queue<int> leaves;
        for (int i = 0; i < n; i++) {
            if (degree[i] == 1) {
                leaves.push(i);
            }
        }
        
        int remainingNodes = n;
        
        // 当剩余节点多于2个时，继续剥离叶子节点
        while (remainingNodes > 2) {
            int leavesCount = leaves.size();
            remainingNodes -= leavesCount;
            
            for (int i = 0; i < leavesCount; i++) {
                int leaf = leaves.front();
                leaves.pop();
                
                // 更新邻居节点的度数
                for (int neighbor : adj[leaf]) {
                    degree[neighbor]--;
                    if (degree[neighbor] == 1) {
                        leaves.push(neighbor);
                    }
                }
            }
        }
        
        // 收集结果
        vector<int> result;
        while (!leaves.empty()) {
            result.push_back(leaves.front());
            leaves.pop();
        }
        
        return result;
    }
};
```